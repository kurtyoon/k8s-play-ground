apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: web-filesystem

patchesJson6902:
  - path: patch-hostpath.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: web-filesystem
