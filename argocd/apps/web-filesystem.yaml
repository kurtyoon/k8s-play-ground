apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-filesystem
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/kurtyoon/k8s-play-ground.git
    targetRevision: main
    path: apps/web-filesystem/k8s/overlays/local
  destination:
    server: https://kubernetes.default.svc
    namespace: web-filesystem
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
